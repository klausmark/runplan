# IP address assigned to an interface on the Docker host, for example its
# Tailscale IPv4 address from `tailscale ip -4`.
RUNPLAN_HOST_IP=100.64.0.1

# Port exposed on the Docker host.
RUNPLAN_HOST_PORT=8000

# Used when a workout step has no explicit pace.
RUNPLAN_DEFAULT_PACE=6:00 min/km
